// import { CELL_UNCOVERED } from '../actions';

const initialState = new Array(10).fill(new Array(10));

const grid = (state = initialState, action) => {
  switch (action.type) {
    // case CELL_UNCOVERED:
      
    default:
      return state;
  }
};

export default grid;
